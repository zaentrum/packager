apiVersion: v1
kind: Service
metadata:
  name: packager
  namespace: stube
  labels:
    app: packager
spec:
  type: ClusterIP
  selector:
    app: packager
  ports:
    - name: http
      port: 80
      targetPort: 8080
